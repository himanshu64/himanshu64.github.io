const varOperations = {
    add(x, y) {
        return parseInt(x) + parseInt(y);

    },
    div(x, y) {
        return parseFloat(x) / parseFloat(y);

    },
    mul(x, y) {
        return parseInt(x) * parseInt(y);
    },
    sub(x, y) {
        return parseInt(x) - parseInt(y);
    }
}